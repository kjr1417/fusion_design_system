Link is for inline, in-sentence navigation — not for button-shaped actions (use Button for those).

```jsx
<Link href="/accounts">View all accounts</Link>
```

underline="default" underlines at rest and removes it on hover; underline="never" never underlines (use sparingly, e.g. in nav rails).
