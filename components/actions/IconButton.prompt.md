IconButton is a square, icon-only Button — for toolbars, dense rows and compact controls where a text label would not fit.

```jsx
<IconButton appearance="transparent" aria-label="Close"><CloseIcon /></IconButton>
```

Always pass aria-label (there is no visible text). Reuses Button's appearance/sentiment variants.
