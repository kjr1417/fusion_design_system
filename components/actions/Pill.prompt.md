Pill is a small chip for filters, tags, and selected values (e.g. in a PillInput).

```jsx
<Pill clickable>Equities</Pill>
<Pill onClose={() => {}}>NASDAQ</Pill>
```

clickable renders a `<button>` with hover/active states; pass onClose to add a removable (x) chip instead.
