Tabs switches between sibling views within the same context (e.g. Overview / Transactions / Documents on an account page).

```jsx
<Tabs items={["Overview", "Transactions", "Documents"]} onChange={(i) => setTab(i)} />
```
