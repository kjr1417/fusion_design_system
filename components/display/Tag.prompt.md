Tag is a small, non-interactive color-coded label for metadata (category, status word) — use Pill instead when it needs to be clickable or removable.

```jsx
<Tag color="accent">NLP</Tag>
<Tag color="positive">Approved</Tag>
```
