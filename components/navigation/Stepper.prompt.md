Stepper shows progress through a fixed, ordered sequence — onboarding, agent-builder setup, multi-page forms.

```jsx
<Stepper steps={["Connect data", "Configure agent", "Review", "Publish"]} activeStep={1} />
```
