Drawer shows detail or a secondary task alongside the current view without fully replacing it — record detail panels, filters.

```jsx
<Drawer open={open} title="AI Research Adoption" side="right" onClose={() => setOpen(false)}>...</Drawer>
```
