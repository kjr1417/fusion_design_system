Tooltip clarifies an icon-only control or truncated label on hover — never put required information only in a tooltip.

```jsx
<Tooltip content="Deploy to staging"><IconButton aria-label="Deploy"><RocketIcon /></IconButton></Tooltip>
```
