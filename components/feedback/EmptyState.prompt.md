`EmptyState` fills a blank list/panel with a custom animated flat SVG (gently floating), a title, optional description, and an optional action.

```jsx
<EmptyState
  title="No knowledge bases yet"
  description="Create your first knowledge base to make curated content discoverable to AI agents."
  action={<Button appearance="solid" sentiment="accented">Create knowledge base</Button>}
/>
```
