`LoadingState` is a full loading placeholder with a custom animated SVG (sequenced accent bars, "AI thinking" feel). Use it for whole-panel/page loads; use `Spinner` for small inline spinners.

```jsx
<LoadingState label="Deploying agent…" />
```
