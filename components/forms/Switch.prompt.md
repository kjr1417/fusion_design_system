Switch applies a setting immediately (no explicit save step) — use Checkbox instead inside a form that's submitted together.

```jsx
<Switch label="Enable dark mode" defaultChecked onChange={(v) => setDark(v)} />
```
