Dropdown collapses a longer list of mutually-exclusive options into one control — prefer RadioButtonGroup for 2-6 always-visible options instead.

```jsx
<Dropdown placeholder="Choose a data source" options={[{ label: "Outlook / Exchange", value: "outlook" }, { label: "Gmail", value: "gmail" }]} />
```
