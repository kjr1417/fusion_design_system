# Fusion Design System — project rules

- **Always left-align text.** Never center-align headings, subheads, body copy, labels, or card content (no `text-align:center`, no centered flex/grid for text blocks). Text is left-aligned throughout unless the user explicitly asks otherwise.
